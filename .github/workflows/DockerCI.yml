name: Docker-CI

on:
  push:
    branches: [ main ]

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build Docker Image
        run: sudo docker build -t live-crypto:latest .

      - name: Run Docker Container
        run: sudo docker run liveCrypto:latest
